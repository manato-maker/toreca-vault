import assert from'node:assert/strict';import fs from'node:fs';const ro=fs.readFileSync(new URL('../js/v2-readonly.js',import.meta.url),'utf8'),app=fs.readFileSync(new URL('../js/app.js',import.meta.url),'utf8');assert.match(ro,/automationHealth:structuredClone/);assert.match(ro,/automationHealth:snapshot\.automationHealth/);assert.match(app,/function automationHealthHtml/);assert.match(app,/Gmail抽選/);assert.match(app,/V2 heartbeat/);assert.match(app,/v2AutomationHealth=applied\.automationHealth/);console.log('automation health UI: ok');