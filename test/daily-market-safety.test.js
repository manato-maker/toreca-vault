import assert from'node:assert/strict';import fs from'node:fs';const code=fs.readFileSync(new URL('../automation/Code.gs',import.meta.url),'utf8');assert.match(code,/marketTrend = result\.price > old \? 'up'/);assert.match(code,/marketFresh = false/);assert.match(code,/取得失敗・前回価格維持/);assert.match(code,/完全一致なし・前回価格維持/);assert.match(code,/marketTrend = 'same'/);console.log('daily market safety: ok');