import test from'node:test';import assert from'node:assert/strict';import{acceptanceSnapshot}from'../v2/acceptance.js';
const state={schemaVersion:2,revision:3,lastMutationId:'m3',transactions:[{id:'p',type:'purchase',price:100,quantity:1},{id:'s',type:'sale',price:150,quantity:1,fee:0,acquisitionCost:100},{id:'o',type:'opening'}],inventoryLots:[{id:'b',product:'B',productKey:'b',category:'BOX',condition:'あり',quantity:2,unitCost:50}],lotteries:[{id:'l'}],marketQuotes:[]};
test('acceptance snapshot reconciles canonical counts and inventory quantity',()=>{const x=acceptanceSnapshot(state);assert.equal(x.ok,true);assert.deepEqual(x.counts,{transactions:3,purchases:1,sales:1,openings:1,lotteries:1,inventoryRecords:1,inventoryQuantity:2});assert.equal(x.assets.total,100);assert.equal(x.realizedProfit,50);assert.equal(x.revision,3)});
test('acceptance snapshot never exposes inventory store dimension',()=>{const x=acceptanceSnapshot({...state,inventoryLots:[{...state.inventoryLots[0],store:'legacy'}]});assert.equal(x.ok,true)});
