import assert from'node:assert/strict';import fs from'node:fs';const w=fs.readFileSync(new URL('../.github/workflows/apps-script-deploy.yml',import.meta.url),'utf8');assert.match(w,/CLASPRC_JSON/);assert.match(w,/TV2_SCRIPT_ID/);assert.match(w,/environment: v2-apps-script/);assert.match(w,/node --test test\/\*\.test\.js/);assert.match(w,/clasp push --force/);assert.match(w,/needs: test/);const m=JSON.parse(fs.readFileSync(new URL('../automation/appsscript.json',import.meta.url),'utf8'));assert.equal(m.timeZone,'Asia/Tokyo');assert.equal(m.runtimeVersion,'V8');console.log('Apps Script deploy workflow: ok');