import assert from'node:assert/strict';import{currentBoxMarketQuotes,currentBoxMarketMeta}from'../data/current-box-market.js';assert.equal(currentBoxMarketMeta.checkedAt,'2026-09-24');assert.equal(currentBoxMarketQuotes.length,3);assert.ok(currentBoxMarketQuotes.every(x=>x.condition==='あり'));assert.ok(currentBoxMarketQuotes.every(x=>Number(x.price)>0&&x.checkedAt&&x.source));assert.equal(currentBoxMarketQuotes.find(x=>x.product.includes('30th CELEBRATION')).price,21000);console.log('current BOX market dataset: ok');
