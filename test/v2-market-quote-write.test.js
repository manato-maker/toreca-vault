import test from'node:test';import assert from'node:assert/strict';import{emptyV2}from'../v2/core.js';import{applyMarketQuoteBatch}from'../v2/market-quote-write.js';
test('market batch changes only quotes and revision metadata',()=>{const s=emptyV2();s.revision=4;s.transactions=[{id:'t'}];s.inventoryLots=[{id:'l',product:'A',category:'BOX',condition:'あり',quantity:1}];s.lotteries=[{id:'x'}];const n=applyMarketQuoteBatch(s,[{product:'A',condition:'あり',price:12000,checkedAt:'2026-09-24',source:'公開相場'}],'m');assert.equal(n.revision,5);assert.equal(n.marketQuotes[0].price,12000);assert.equal(n.marketQuotes[0].checkedAt,'2026-09-24');assert.deepEqual(n.transactions,s.transactions);assert.deepEqual(n.inventoryLots,s.inventoryLots);assert.deepEqual(n.lotteries,s.lotteries)});
test('older quote cannot overwrite newer quote',()=>{const s=emptyV2();s.marketQuotes=[{product:'A',condition:'あり',price:15000,checkedAt:'2026-09-24'}];const n=applyMarketQuoteBatch(s,[{product:'A',condition:'あり',price:10000,checkedAt:'2026-09-17'}],'m');assert.equal(n.marketQuotes[0].price,15000);assert.equal(n.revision,0)});
test('quote requires acquisition date',()=>{const s=emptyV2();assert.throws(()=>applyMarketQuoteBatch(s,[{product:'A',price:1}],'m'),/取得日/)});
